import { ChangeBackgroundHoverDirective } from './change-background-hover.directive';

describe('ChangeBackgroundHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBackgroundHoverDirective();
    expect(directive).toBeTruthy();
  });
});
