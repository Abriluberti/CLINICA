import { HoverTraceDirectiveDirective } from './hover-trace-directive.directive';

describe('HoverTraceDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverTraceDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
