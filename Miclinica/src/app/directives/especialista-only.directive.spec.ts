import { EspecialistaOnlyDirective } from './especialista-only.directive';

describe('EspecialistaOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new EspecialistaOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
